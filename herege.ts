import { Elementos } from "./Elementos";

export class herege extends Elementos {

    constructor(nome: string, n_atomico: number, simbolo: string, curiosidade: string){
        super(nome, n_atomico, simbolo, curiosidade);
    }   
}